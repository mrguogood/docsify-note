# 📖 操作指南

> 本文档网站使用 **Docsify** 构建，以下是常用操作指南。

---

## 🚀 本地运行

### 环境要求

- Node.js >= 12.x
- npm >= 6.x

### 安装 Docsify CLI

```bash
npm i docsify-cli -g
```

### 启动本地服务

```bash
# 在项目根目录执行
docsify serve docs

# 或指定端口
docsify serve docs --port 3001
```

启动成功后，访问 [http://localhost:3000](http://localhost:3000) 即可预览。

---

## 📝 编写文档

### 创建新文档

1. 在 `docs` 目录下创建 `.md` 文件
2. 在 `_sidebar.md` 中添加导航链接

### Markdown 语法支持

本站支持标准 Markdown 语法以及以下扩展：

| 功能 | 语法 |
|------|------|
| 表格 | `\| 列1 \| 列2 \|` |
| 代码高亮 | ` ```java ` |
| 任务列表 | `- [x] 完成` |
| 数学公式 | `$E=mc^2$` |
| Emoji | `:smile:` |

---

## 🏗️ 项目结构

```
docs/
├── index.html          # 入口文件
├── README.md           # 首页内容
├── _sidebar.md         # 侧边栏配置
├── _navbar.md          # 导航栏配置
├── _coverpage.md       # 封面配置
├── guide.md            # 操作指南
├── assets/             # 静态资源
│   └── css/
│       └── custom.css  # 自定义样式
├── images/             # 图片资源
├── 前端技术/           # 前端文档
├── 后端技术/           # 后端文档
├── 数据库/             # 数据库文档
├── Typora+Docsify/     # 工具文档
└── 其他/               # 其他文档
```

---

## 🔧 配置说明

### 主要配置项 (index.html)

```javascript
window.$docsify = {
  name: '📚 技术笔记',     // 网站名称
  repo: 'github-url',      // 仓库地址
  loadSidebar: true,       // 加载侧边栏
  loadNavbar: true,        // 加载导航栏
  coverpage: true,         // 显示封面
  maxLevel: 4,             // 最大标题层级
  subMaxLevel: 3,          // 侧边栏目录层级
  auto2top: true,          // 自动回到顶部
}
```

### 插件列表

本站已集成以下插件：

| 插件 | 功能 |
|------|------|
| search | 全文搜索 |
| zoom-image | 图片缩放 |
| copy-code | 代码复制 |
| pagination | 分页导航 |
| countable | 字数统计 |
| sidebar-collapse | 侧边栏折叠 |
| emoji | Emoji 表情 |
| katex | 数学公式 |
| progress | 阅读进度条 |

---

## 🚢 部署指南

### GitHub Pages 部署

1. 将代码推送到 GitHub 仓库
2. 进入仓库设置 → Pages
3. Source 选择 `main` 分支，目录选择 `/docs`
4. 保存后等待部署完成

### 自定义域名

在 `docs` 目录下创建 `CNAME` 文件，写入你的域名：

```
your-domain.com
```

---

## 📱 快捷键

| 快捷键 | 功能 |
|--------|------|
| `/` | 打开搜索框 |
| `Esc` | 关闭搜索框 |
| `↑` `↓` | 选择搜索结果 |
| `Enter` | 跳转到选中结果 |

---

## ❓ 常见问题

### Q: 侧边栏不显示？

确保 `index.html` 中配置了 `loadSidebar: true`，并且存在 `_sidebar.md` 文件。

### Q: 图片无法显示？

检查图片路径是否正确，建议使用相对路径，如：`./images/xxx.png`

### Q: 搜索不工作？

确保已引入搜索插件脚本：
```html
<script src="//cdn.jsdelivr.net/npm/docsify/lib/plugins/search.min.js"></script>
```

---

## 📚 参考链接

- [Docsify 官方文档](https://docsify.js.org)
- [GitHub Pages 文档](https://pages.github.com)
- [Markdown 语法指南](https://www.markdownguide.org)

---

<p style="text-align: center; color: #999; margin-top: 50px;">
  如有问题，欢迎提交 <a href="https://github.com/mrguogood/docsify-note/issues" target="_blank">Issue</a>
</p>
